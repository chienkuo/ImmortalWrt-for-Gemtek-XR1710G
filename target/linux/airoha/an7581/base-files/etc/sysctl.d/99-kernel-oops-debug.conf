# The generic base-files profile sets kernel.panic=3 at boot. Keep AN7581
# failures visible on the console so wireless driver Oops traces can be
# captured instead of rebooting immediately.
kernel.panic_on_oops=0
kernel.panic=0
